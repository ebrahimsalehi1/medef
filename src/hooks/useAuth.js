import { useContext } from 'react';
//import AuthContext from 'src/contexts/JWTAuthContext';

//const useAuth = () => useContext(AuthContext);
const useAuth = () => useContext(null);

export default useAuth;
