import { useContext } from 'react'; 
//import SettingsContext from 'src/contexts/SettingsContext';

//const useSettings = () => useContext(SettingsContext);
const useSettings = () => useContext(null);

export default useSettings;
